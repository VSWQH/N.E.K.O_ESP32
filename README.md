# N.E.K.O ESP32 Source

This repository mirrors the two local project folders:

- `app-esp32/`
- `ESP32_AI_LLM-master/`

Runtime configuration files that may contain API keys, memory, locations, uploaded audio, or build products are intentionally excluded. Use the included `*.example.json` files as local templates.
